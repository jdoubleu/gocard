import React from "react";
import Headline from "../shared/headline";

const Help = () => {
    return (
        <div>
            <Headline title="Hilfe"/>
        </div>
    );
};

export default Help;
